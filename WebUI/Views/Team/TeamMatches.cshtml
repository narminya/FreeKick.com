@model TeamsMatchesViewModel
@{
    ViewData["Title"] = "TeamMatches";
}
<partial name="_TeamPartial" model="@Model.Team" />
<section class="team-matches p-50">
    <div class="container">
        <div class="row">
            <div class="col-lg-8">
                <div class="table-ms">
                    <div class="title-head d-flex">
                        <a href="">All</a>
                        <a href="">UEFA Champions League</a>
                        <a href="">Premier League</a>
                    </div>
                    <div class="match-list">
                        @foreach (var item in Model.Matches)
                        {
                            <div class="match-t">

                                @if (item.Date < DateTime.Now)
                                {<div class="match-status">
                                        FT
                                    </div>
                                }
                                else
                                { <div class="match-status">
                                        @item.Date.ToShortTimeString();
                                    </div>          }

                                <div class="match-data">
                                    <p class="team-home">@item.Home.TeamName</p>
                                    <div class="goals d-flex ">
                                        <div class="match-result">
                                            <div class="goals-home">@item.Home.TeamScore</div>
                                            <span>-</span>
                                            <div class="goals-away">@item.Away.TeamScore</div>
                                        </div>

                                    </div>
                                    <p class="team-away">@item.Away.TeamName</p>
                                </div>
                            </div>
                        }

                    </div>

                </div>
            </div>
            <div class="col-lg-4">
                <img src="/assets/img/jawn300x500.jpg" alt="">
            </div>
        </div>
    </div>
</section>

